import { Router } from 'express';

const router = Router();

/*

router.post('/actions/upload-problem', ); // Send a problem, receive first question from teacher cat
router.post('/actions/teacher-cat-message', ); // Send teacher cat a message, receive one back
router.post('/actions/teacher-cat-done', ); // Boolean for done or not, handled accordingly
router.post('/actions/student-cat-message', ); // Send student cat a message, receive one back
                                               // If done, include flag and points earned

*/

export default router;